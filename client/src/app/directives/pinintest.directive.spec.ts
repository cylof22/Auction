import { PinintestDirective } from './pinintest.directive';

describe('PinintestDirective', () => {
  it('should create an instance', () => {
    const directive = new PinintestDirective();
    expect(directive).toBeTruthy();
  });
});
